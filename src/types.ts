export interface SearchResponse {
  flag: string;
  population: number;
  region: string;
  capital: string;
  name: string;
}

export interface Card {
  flag: string;
  name: string;
  region: string;
  capital: string;
  population: number;
}
